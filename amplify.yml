version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Skipping preBuild for now"
    build:
      commands:
        - echo "Minimal build step"
  artifacts:
    baseDirectory: pastor-sermon-app
    files:
      - '**/*'
  cache:
    paths: []