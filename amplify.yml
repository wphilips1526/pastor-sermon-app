version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd PastorSermonApp
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: PastorSermonApp
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*