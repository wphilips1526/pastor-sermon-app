version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd pastor-sermon-app
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: pastor-sermon-app
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*